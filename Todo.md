# DoraPocket TODO

这里只记录当前仍未完成、并且值得进入产品或工程计划的事项。已完成的功能、旧方案和未经验证的愿望不留在清单中；长期方向只在 `README.md` 保留一句话，具体任务放在这里。

## Now

### P0：安全与运行边界

- [ ] **复用生产环境的 Prisma Client**：目标是避免每次访问模型属性都创建新的 client/连接池；完成标准是生产条件下连续访问多个 model 只创建一个 client，并有回归测试。
- [ ] **限制外部 URL 与资源抓取**：目标是防止工具提交、favicon 和重定向访问私网、回环地址或云元数据地址；完成标准是校验解析后的 IPv4/IPv6、逐次重定向和响应体大小，并覆盖失败测试。
- [ ] **收紧 OAuth 回跳地址**：目标是只允许站内路径，拒绝 `//host` 等协议相对外链；完成标准是 callback、登录入口和异常回跳均通过同一套安全校验。
- [ ] **增加 Chat 与语音接口的应用层额度**：目标是限制匿名调用、单用户调用、输入长度、并发和模型成本；完成标准是非法输入返回明确的 4xx，超额请求不会进入模型或语音服务。

## Next

### P1：数据语义与产品信任

- [ ] **落实 `memoryEnabled` 的真实语义**：目标是分别定义“停止新增记录”和“停止使用历史偏好”，并在所有推荐入口一致执行；完成标准是关闭、重新开启和匿名三类场景都有测试，界面承诺与服务端行为一致。
- [ ] **修正推荐反馈的行为含义**：目标是区分实际打开、用户自报试用、是否有帮助、失败原因和未知状态；完成标准是每个按钮只写入实际发生的字段，偏好推断不会把未确认行为当作正向成功信号。
- [ ] **建立投稿审核到发布的状态流**：目标是普通投稿先进入待审核候选，不直接发布或覆盖既有工具；完成标准是覆盖新投稿、同 URL 重投、重复判定、审核发布和失败回滚。
- [ ] **记录外部候选的真实行为**：目标是让模型生成的外部建议拥有来源和会话内候选标识；完成标准是外部候选的打开、收藏或失败可与库内工具区分统计。

### P1：失败恢复与推荐链路

- [ ] **补齐市场查询和提交错误态**：目标是区分加载中、空结果和接口错误，并在提交失败时保留草稿；完成标准是目录 500、提交 401/500、无匹配搜索和有收藏补全等场景都有可恢复 UI。
- [ ] **建立推荐质量基线**：目标是用固定任务集衡量适配度、可启动性、成功率、复用价值和信任感；完成标准是先定义事件和隐私边界，再得到可重复的初始数据，不能用“工具数量”代替推荐质量。
- [ ] **补充真实流式输出与取消**：目标是让阶段事件和回答输出反映真实进度，并在客户端断开时取消模型调用；完成标准是首条有效内容耗时、请求取消和会话持久化一致性可验证。

### P2：规模边界与可维护性

- [ ] **限制工具目录和用户上下文规模**：目标是为公开目录分页、缓存和失效策略，为推荐上下文设置有界读取；完成标准是查询不再无界读取全量数据，私人上下文不会进入跨用户共享缓存。
- [ ] **完善后台同步的可观测性**：目标是让 Webhook、Cron、embedding、favicon 和投稿去重的成功、跳过、失败可追踪；完成标准是失败有上下文、可重试，并能区分业务失败与外部服务失败。

## Later / Explore

- [ ] **评估 RAG 与任务经验库**：先验证收藏、用户提交、反馈和“什么人在什么限制下尝试后有效”的数据是否足以形成可复用知识，再决定是否扩展 embedding/RAG 体系。
- [ ] **评估推荐实验与 LangSmith**：在有固定任务集和基础事件后，再判断是否引入 LangSmith 或自建评测集。
- [ ] **评估 Web Worker 与传输协议优化**：先用性能数据确认 UI 重计算和当前 NDJSON 链路的瓶颈，再决定是否引入 Web Worker 或 SSE。
- [ ] **评估 PostHog / Sentry**：以错误恢复、推荐漏斗和隐私边界为前提，确认收益后再接入观测服务。
- [ ] **按真实问题优化 3D 与 UI 原语**：只在有明确性能或交互问题时优化 Three.js、动画或共享 UI 组件，不以增加组件数量作为目标。
