/**
 * DoraPocket 全站页面外壳布局常量。
 * 新页面请使用 `PageShell` 组件，默认即套用下列宽度与水平留白；
 * 除非有明确例外，勿单独覆盖 max-width / 水平 padding。
 */

/** 全站主内容区最大宽度（约 1920px） */
export const PAGE_SHELL_MAX_WIDTH_CLASS = 'max-w-[min(100%,120rem)]'

/** 全站主内容区与顶栏一致的水平内边距 */
export const PAGE_SHELL_CONTENT_X_CLASS = 'px-3 sm:px-4 lg:px-4'
