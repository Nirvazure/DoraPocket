<!-- BEGIN:nextjs-agent-rules -->

# DoraPocket Agent Rules

## 必读文档

- 动手修改前，先阅读 `GUIDE.md` 中与任务相关的章节。
- `GUIDE.md` 是 DoraPocket 产品未来演进策略、阶段路线、近期优先级与 UED 原则的唯一依据。
- `README.md` 只维护项目概览、当前状态、本地启动与文档索引。
- 若产品方向、交互原则或优先级出现冲突，以 `GUIDE.md` 为准。

## AI 协作边界

- 不擅自扩大需求范围，不把局部任务扩展成未授权的产品重构。
- 不把 DoraPocket 改成聊天壳、纯工具目录、纯 RAG 百科、普通榜单、收藏夹或角色秀场。
- 需求不明确时，先研究现状并给出计划，不直接改代码。
- 涉及产品方向、推荐逻辑、市场、口袋、Agent、UED 或信息流的调整时，先对照 `GUIDE.md`。
- 对任何可能削弱“需要帮助时拿出合适帮助”的改动，应默认降级或先向用户确认。

## Next.js 特殊注意事项

- This is NOT the Next.js you know.
- 当前项目所用 Next.js 版本可能包含与你训练数据不同的 API、约定和目录结构。
- 在编写或修改 Next.js 相关代码前，先阅读 `node_modules/next/dist/docs/` 中对应文档，并注意废弃项与破坏性变更。

## 工程修改规则

- 优先修根因，不做表层补丁。
- 保持改动最小，避免无关重构。
- 不修无关问题；发现无关问题时只在总结中说明。
- 不擅自新增重后端、鉴权、数据库或持久化系统。
- 不在面向用户的界面中暴露工程内部 ID，例如 `toolId`。
- 外部工具优先作为推荐资产，不强行内化执行。
- 原生卡片仅保留高频、低摩擦、小闭环、复用收益明确的能力。
- 修改推荐、市场、口袋、Agent 或 UED 时，必须保持解释层与执行层一致。

## 推荐与体验底线

- 帮助优先于工具：用户需要的是此刻最合适的帮助，不只是工具条目。
- 裁决优先于推荐：先给结论，再给理由，再给动作。
- 结果优先于过程：不要让状态播报、诊断信息或过程展示抢占结论权威。
- 角色和语音是增强层，不是主任务流中心。
- 推荐必须可解释，不能只依赖热度、点赞数或平均分。
- 推荐卡片、市场卡片、候选卡与口袋沉淀动作应服务同一套裁决逻辑。

## 文档维护规则

- `GUIDE.md`：产品未来演进策略、阶段路线、近期优先级、UED 原则唯一依据。
- `README.md`：项目概览、当前状态、本地启动、文档索引。
- `AGENTS.md`：AI 协作边界、工程修改规则、必要硬约束。

## 提交卫生规则

- 提交前必须清理 CRLF 噪音、trailing whitespace 和无意义格式化；文本文件默认使用 LF。
- 不提交临时设计原型、截图草稿、一次性 HTML、无引用 demo 图片或本地生成产物。
- `src/generated/prisma/` 只在 `prisma/schema.prisma` 或 Prisma 版本真实变化后提交；如果只是生成器格式噪音，应从暂存区移除。
- 修改 `.husky/*` 时保持可执行脚本逻辑最小化，避免只因为换行符变化制造提交噪音。

<!-- END:nextjs-agent-rules -->
