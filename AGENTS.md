<!-- BEGIN:nextjs-agent-rules -->
# DoraPocket Agent Rules

## 项目认知

- DoraPocket 是“陪伴式能力口袋”，当前第一阶段以工具裁决与复用 Agent 落地。
- 核心价值是：在用户需要帮助时理解处境、比较候选帮助、做出裁决、解释理由、给出下一步动作，并把高价值帮助沉淀成资产。
- DoraPocket 的长期目标不是静态目录，而是一个会随着使用不断变准、越来越会出手的动态帮助系统。
- 任何让项目更像聊天壳子、表演型 Demo、纯工具陈列页、纯 RAG 百科、收藏夹升级版或只有情绪陪聊的产品修改，都视为方向偏离。

## Next.js 特殊注意事项

- This is NOT the Next.js you know.
- 当前项目所用 Next.js 版本可能包含与你训练数据不同的 API、约定和目录结构。
- 在编写或修改 Next.js 相关代码前，先阅读 `node_modules/next/dist/docs/` 中对应文档，并注意废弃项与破坏性变更。

## AI 开发总原则

- 优先强化主任务流，不要强化表演层。
- 优先强化“需要帮助时拿出合适帮助”的体验，不要只堆推荐与展示。
- 优先强化情境化裁决与复用，不要把产品做成纯目录、纯 RAG 或纯陪聊。
- 推荐系统优化目标不是最大化点击，而是最大化帮助有效性。
- 优先提高结果权威，不要过度展示过程。
- 优先把体验做得更清晰、更可复用，不要仅仅做得更炫。
- 若一个改动不能让 DoraPocket 更像“帮用户在此刻得到合适帮助”的产品，其优先级应被下调。

## 产品方向硬约束

- 市场数据当前优先走本地闭环，不默认引入重后端、鉴权和持久化系统。
- Agent 当前优先聚焦“找工具 + 解释 + 排序”，不追求全自动工作流。
- 外部工具优先作为推荐资产，不强行内化执行；原生卡片仅保留高频、低摩擦、小闭环能力。
- RAG 可以作为世界记忆与知识底座，用于长尾召回、语义理解和解释依据。
- RAG 不能替代结构化排序、用户偏好、情境判断和裁决逻辑。
- 产品壁垒不是“知识库大”，而是情境化裁决、陪伴式出手和可复用资产沉淀。
- 市场评分不是普通星级榜单，而应服务“帮助是否真的有效”的信号系统。
- 不得用单一热度、点赞数、平均分替代情境判断与用户适配。
- 用户系统、持久化数据库和事件记录的存在意义，是让 DoraPocket 更准地出手，而不是堆积基础设施。
- `meta -> delta -> done` 的结构化输出优先于细碎流式炫技，前端必须能拿到候选、理由与 `ui_payload`。
- 3D 角色与语音链路是增强层，不是产品中心，不得抢占主入口和主解释权。
- 自动沉淀必须可撤销、可关闭、可恢复，不能默认污染口袋。
- 推荐必须暴露偏好与市场信号，不得做黑盒排序。
- 面向用户的界面中不得直接暴露工程内部 ID，例如 `toolId`。

## UED 与信息流硬约束

- 首页首屏只保留一个绝对主角，先给结论，再给比较与动作。
- 用户表达困难后，系统应快速形成“掏口袋时刻”：拿出一个可行动帮助，而不是先抛出大量候选。
- 解释要短、明确、有温度，语气接近“我懂，你现在先用这个”。
- 右侧区域不再承担主裁决表达，不得重新膨胀为主舞台。
- 提示系统必须分等级：只有必要信息常驻，其余信息应弱化、短暂出现或自动退场。
- 同一事件不得在多个区域重复播报；结论、原因、动作应各司其职。
- Agent Insight / 推荐结果区必须具备比普通信息卡更高的视觉与叙事优先级。
- 市场页的职责是“比较依据空间”，不是“逛工具广场”。
- 口袋页的职责是“复用空间”，不是普通收藏夹。
- 跨页体验必须维持同一视觉系统与空间连续感，不要做成多个产品拼接。

## 排序、解释、偏好与反馈规则

未来做推荐、排序、解释和反馈设计时，应始终围绕帮助有效性的五个维度：

- 适配度：是否适合当前任务、当前用户和当前限制条件
- 可启动性：是否容易立刻开始，是否摩擦过高
- 成功率：是否更可能真正解决问题
- 复用价值：是否值得进入口袋并再次使用
- 信任感：用户是否理解并相信这次推荐

若某个设计只提升热度表现，却削弱上述五个维度，应判定为方向偏移。

- Agent 面板与市场排序必须共享同一候选顺序。
- 候选卡必须支持跳转、收藏、订阅、打开等快捷动作，缩短闭环。
- 推荐不仅要解释“为什么选它”，还要解释“为什么不是另外几个”。
- 裁决信号要前推到列表卡与候选卡，不应只藏在详情页。
- 用户偏好不能只停留在行为日志，必须能转化为排序可用画像。
- 显式偏好校准优先于被动推断，并且必须即时作用于当前结果。
- 偏好校准后的变化必须可感知，用户需要看到哪些候选上升或下降，以及原因是什么。
- 结果反馈必须回流排序；系统要关注用户最后用了谁、复用了谁、放弃了谁，而不只是点了谁。
- 负向信号的价值高于泛化点赞；“不适合我”“太复杂”“太贵”应比随手喜欢更能影响排序。
- 复用信号的权重高于收藏信号；再次使用比“先存着”更能证明帮助价值。
- 情境判断不能被全站热度覆盖；热门不代表此刻适合。
- 用户必须能感知推荐理由；不允许系统只在内部算分，却不给用户合理解释。
- 推荐卡片、对比层、详情层、口袋沉淀动作，都应体现“为什么适合这次”的有效性说明。
- 帮助单元优先于工具条目；未来若出现模板、流程、提示词或组合方案，应按同一推荐逻辑处理。
- 推荐系统的升级不得破坏陪伴式、克制、少打扰的品牌气质。

## 当前阶段的已确认方向

- 首页首屏保留单一主角 `DecisionHero`。
- 首页第一屏先给结论，再给比较与动作。
- 首页指标卡已移出首屏，由成长页承接。
- 快速开始建议已缩减为 3 条。
- 性能信息已并入记录抽屉，不应重新回到主界面中心。
- 语音提示仅在输入时短暂出现，文字转录提示不再常驻主链路。
- 市场页已独立并服务选型比较。
- 口袋页已明确为“复用空间”。
- 首页 / 市场 / 口袋 / 成长页已共享统一顶栏骨架。

## 当前仍需注意的收尾项

- 右侧仍有进一步压缩空间，但只能继续降级，不能回摆。
- `pocket-page` 中残留的旧市场分支代码需要逐步清理。
- 跨页转场节奏与空间连贯感仍需继续细化。
- 参数模板定位、任务起手式、个人中心实化、内置工具入口路径等问题应优先按主线收敛处理。

## 禁止事项

- 不要把 DoraPocket 改回“万能 AI 聊天入口”叙事。
- 不要把 DoraPocket 做成纯 RAG 工具百科。
- 不要把 DoraPocket 做成纯 AI 工具目录。
- 不要把市场做成普通排行榜或只拼热度的榜单。
- 不要把评分系统做成只有平均分和点赞数的粗糙机制。
- 不要把数据库、用户系统、RAG 建设做成脱离产品价值的基础设施工程。
- 不要只追求收录更多工具，而忽略信号质量、反馈闭环与情境适配。
- 不要为了可视化系统复杂度，而把前台体验做成算法控制台。
- 不要把 DoraPocket 做成只有陪聊、没有具体帮助的情绪产品。
- 不要只给很多候选，却不给“这次先用它”的明确出手。
- 不要把角色、语音、动效做成主任务流中心。
- 不要为了看起来更智能而叠加伪 Agent 流程。
- 不要做纯陈列型市场页、纯收藏型口袋页、纯展示型原生工具卡。
- 不要让解释层和执行层排序不同。
- 不要在没有明确收益的情况下扩大工具数量或扩展非核心场景。
- 不要重新把性能诊断、状态播报、转录提示等信息拉回主叙事中心。

## 文档维护规则

- `README.md` 维护项目概览与当前状态，不写过细规则。
- `AGENTS.md` 维护 AI 开发边界与稳定约束，不写长篇宣言。
- `YOLO_FUTURE.md` 维护未来方向与长期原则，不写短期待办。
- `TODO.md` 只维护近阶段可执行事项。
- `ROADMAP.md` 只维护阶段路线与里程碑。

若一个需求或改动出现分歧，请先自检：

- 它是否让 DoraPocket 更像工具裁决产品？
- 它是否让用户更快完成选型并更愿意回来？
- 它是否让口袋更像资产系统？
- 它是否让主价值更锋利，而不是更分散？

若以上问题中有两项及以上无法回答“是”，默认不应优先开发。
<!-- END:nextjs-agent-rules -->
